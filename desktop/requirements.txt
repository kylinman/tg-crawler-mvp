# Desktop Qt cross-platform admin UI for TG Crawler
#
# IMPORTANT: Never run plain "pip install -r requirements.txt" against the
# system Python on modern distros (Arch, recent Ubuntu/Fedora etc.).
# This will trigger "externally-managed-environment" (PEP 668).
#
# Best ways:
#
# 1. Use the project's one-click setup (creates isolated .venv like web/crawler):
#      ./scripts/local/setup-python.sh
#      source desktop/.venv/bin/activate
#      python desktop/main.py
#
# 2. Use uv (recommended by you, fast & modern):
#      cd desktop
#      uv venv --python 3.11 .venv
#      source .venv/bin/activate
#      uv pip install -r requirements.txt
#      python main.py
#
# The setup-python.* scripts were updated to support "desktop/" automatically.

PySide6>=6.6.0
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0
boto3>=1.34.0
Pillow>=10.0.0
# Optional: for better image handling or future features
# requests>=2.31.0
